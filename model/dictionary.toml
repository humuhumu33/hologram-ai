# The hologram-ai dictionary, transcribed row-by-row from
# docs/conceptual-model/00-source.md. This is the single source of truth the BDD
# features and V&V witnesses are checked against by the honesty meta-gate.
#
# Fields:
#   id        kebab-case row identifier (matches the feature tag @row:<id>)
#   statement the one-line normative claim
#   stage     S0 addressing | S1 acquisition | S2 compilation | S3 execution | S4 application
#   status    verified | build | open           (see model/status.toml)
#   oracles   ids from model/oracles.toml
#   tier      suite  = implemented, gating, green
#             target = defined behavior, measured/probed, non-gating
#   executor  rust    = cucumber (hologram-ai-conformance)
#             browser = cucumber-js + Playwright Chromium (apps/web/bdd)
#   feature   path (relative to repo root) of the Gherkin definition

# ───────────────────────────── S0 — addressing ─────────────────────────────

[[row]]
id = "kappa-addressing"
statement = "A buffer's κ-label is `blake3:<hex>` of its bytes, reproducing the official BLAKE3 vectors and `holospaces::address`."
stage = "S0"
status = "verified"
oracles = ["blake3-kats", "holospaces-substrate"]
tier = "suite"
executor = "rust"
feature = "features/suites/s0_addressing/kappa_addressing.feature"

[[row]]
id = "chunked-kappa-persisting"
statement = "Incremental (chunked) κ-hashing of a stream equals the one-shot κ of the whole content, for every chunking."
stage = "S0"
status = "verified"
oracles = ["blake3-kats"]
tier = "suite"
executor = "rust"
feature = "features/suites/s0_addressing/chunked_kappa_persisting.feature"

[[row]]
id = "content-verified-resolution"
statement = "Resolving κ → bytes re-hashes the bytes; content that does not reproduce its κ is rejected with the label."
stage = "S0"
status = "build"
oracles = ["blake3-kats"]
tier = "suite"
executor = "rust"
feature = "features/suites/s0_addressing/content_verified_resolution.feature"

# ───────────────────────────── S1 — acquisition ─────────────────────────────

[[row]]
id = "hf-model-resolution"
statement = "Any HuggingFace repo id resolves to its file manifest via the Hub API, classified into shards and companions."
stage = "S1"
status = "verified"
oracles = ["hf-hub"]
tier = "suite"
executor = "rust"
feature = "features/suites/s1_acquisition/hf_model_resolution.feature"

[[row]]
id = "safetensors-header-streaming"
statement = "Files produced by the reference safetensors crate stream-parse (8-byte length + JSON header + ranges) to identical tensors."
stage = "S1"
status = "verified"
oracles = ["safetensors-ref"]
tier = "suite"
executor = "rust"
feature = "features/suites/s1_acquisition/safetensors_header_streaming.feature"

[[row]]
id = "streamed-download"
statement = "The download worker streams shards tensor-by-tensor into the OPFS κ-store; peak transient memory is bounded by one tensor."
stage = "S1"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s1_acquisition/streamed_download.feature"

[[row]]
id = "supported-search"
statement = "Model discovery preflights DERIVABILITY: search candidates run the same config preflight the download journey runs (the wasm binding over the parametric registry + derivation — never a name list in the app); derivable models are selectable, and a model the preflight refuses stays visible, greyed, annotated with the refusal reason verbatim — an honest refusal is information, not something to hide."
stage = "S1"
status = "build"
oracles = ["journey-reference", "hf-hub"]
tier = "suite"
executor = "browser"
feature = "features/suites/s1_acquisition/supported_search.feature"

[[row]]
id = "model-preflight"
statement = "Before any shard byte moves, the model is validated: the config supplies the parametric decoder schema (a recognized family, or an unrecognized architecture derived from its manifest) and the graph builds from config + the header-only manifest; a config or manifest the recipe cannot represent is rejected loud naming the architecture/key, with zero shard bytes transferred."
stage = "S1"
status = "build"
oracles = ["journey-reference", "hf-hub"]
tier = "suite"
executor = "browser"
feature = "features/suites/s1_acquisition/model_preflight.feature"

[[row]]
id = "memory-guard"
statement = "The resource guard is pure projection: κ-store need, measured local headroom, cache coverage, window, and stage plan are surfaced as information before transfer; the journey is never refused for resources."
stage = "S1"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s1_acquisition/memory_guard.feature"

[[row]]
id = "network-skip"
statement = "Transit is the set-difference from the known: under a shard's content pin (its HTTP ETag — the Hub's blob hash), ranges whose κ an earlier stream recorded never re-transit (known = provenance-recorded κ, not cached bytes; unknown runs move as coalesced ranges); a changed pin discards the prior and streams fully; no skipped byte is trusted — every κ verifies at first materialization and a wrong prior unpins and recovers."
stage = "S1"
status = "build"
oracles = ["journey-reference", "hf-hub"]
tier = "suite"
executor = "browser"
feature = "features/suites/s1_acquisition/network_skip.feature"

[[row]]
id = "companion-assets"
statement = "config.json, tokenizer.json, and generation_config.json are fetched and persisted under the model directory."
stage = "S1"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s1_acquisition/companion_assets.feature"

# ───────────────────────────── S2 — compilation ─────────────────────────────

[[row]]
id = "parametric-graph"
statement = "The decoder graph is a function of config.json + the tensor manifest alone (hidden/layers/heads/kv-heads/head-dim/vocab/rope_theta/rms_norm_eps/tying/context/dtype); a recognized family selects its builder and an unrecognized architecture is derived from its manifest, so arbitrary decoders build without a name allowlist while a manifest the recipe cannot faithfully represent fails loud."
stage = "S2"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s2_compilation/parametric_graph.feature"

[[row]]
id = "streamed-weightless-compile"
statement = "Compilation from the manifest (names/shapes/dtypes/κ) alone emits a k-form .holo whose kappa_map binds every weight constant to its κ."
stage = "S2"
status = "build"
oracles = ["hf-hub"]
tier = "suite"
executor = "rust"
feature = "features/suites/s2_compilation/streamed_weightless_compile.feature"

[[row]]
id = "onnx-compile-parity"
statement = "Compiled ONNX graphs execute to ONNX Runtime's outputs within tolerance, on the committed fixtures and the official node corpus."
stage = "S2"
status = "verified"
oracles = ["onnx-runtime", "onnx-node-corpus", "onnx-fixtures"]
tier = "suite"
executor = "rust"
feature = "features/suites/s2_compilation/onnx_compile_parity.feature"

[[row]]
id = "quant-dequant"
statement = "Q4_0/Q8_0 dequantization reproduces the GGML reference golden vectors bit-exactly."
stage = "S2"
status = "verified"
oracles = ["quant-goldens"]
tier = "suite"
executor = "rust"
feature = "features/suites/s2_compilation/quant_dequant.feature"

[[row]]
id = "family-registry-support"
statement = "Every registered architecture family is verified against a pinned published model: the parametric k-form compiles from the model's real config.json and safetensors manifest, its κ-map covering every tensor exactly once."
stage = "S2"
status = "verified"
oracles = ["hf-hub", "smollm2-135m", "mistral-7b-instruct", "phi-4"]
tier = "suite"
executor = "rust"
feature = "features/suites/s2_compilation/family_registry_support.feature"

[[row]]
id = "deterministic-compile"
statement = "The parametric k-form compile is deterministic: identical config + manifest produce a byte-identical archive (a stable κ) — content-addressing requires it, monolithic and staged."
stage = "S2"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s2_compilation/deterministic_compile.feature"

[[row]]
id = "prism-canonical-manifest"
statement = "A compiled archive can bake its prism contract: the canonical hologram-ai/archive/1 manifest (κ = blake3 of exact canonical bytes; compile equivalence = κ equality), admitted at load through strict decode, normalization, and the kernel-extracted validator conjunction; a tampered manifest is refused, an absent one loads unchanged."
stage = "S2"
status = "build"
oracles = ["prism-contract"]
tier = "suite"
executor = "rust"
feature = "features/suites/s2_compilation/prism_canonical_manifest.feature"

[[row]]
id = "parametricity"
statement = "No canonical-instance constant leaks into generic code: an arbitrary use-case instantiates the same pipeline end-to-end, and the anti-hardcode gate holds."
stage = "S2"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s2_compilation/parametricity.feature"

[[row]]
id = "arbitrary-architecture-coverage"
statement = "Coverage of HuggingFace architecture families by the parametric registry — measured and reported, never asserted universal."
stage = "S2"
status = "open"
oracles = ["hf-hub"]
tier = "target"
executor = "rust"
feature = "features/suites/s2_compilation/arbitrary_architecture_coverage.feature"

# ───────────────────────────── S3 — execution ─────────────────────────────

[[row]]
id = "staged-execution"
statement = "Windowed execution over k equals monolithic execution: the stage archives' κ-maps partition the model's tensors exactly, the staged pipeline reproduces the monolithic logits, and peak weight residency is bounded by the window, never the model."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/staged_execution.feature"

[[row]]
id = "staged-window-growth"
statement = "The staged generation window follows the sequence, never the model: stage k-forms are weightless, so the session recompiles them per geometric window bucket up to the model's own context, per-token compute scales with the actual sequence, the regrown pipeline reproduces the fixed-window completion exactly, and a sequence past the model's context fails loud naming both lengths."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/staged_window_growth.feature"

[[row]]
id = "stage-residency-cache"
statement = "κ-store bandwidth is paid per window, never per token, within the environment's measured headroom: materialized stage sessions stay resident across forward passes while they fit the residency budget, each stage materializes once per window, the resident set never exceeds the budget (one stage is the floor), and a zero budget is exactly the strict one-stage window; cached and strict execution produce identical completions."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/stage_residency_cache.feature"

[[row]]
id = "session-verified-kappa"
statement = "Verification is placed at the trust boundary, once per crossing, never per traversal: a κ verifies (re-hashes) at its first materialization in a session and later rematerializations are read-only I/O; the verified set is session-scoped, so a fresh session re-verifies at first touch and rejects corrupted content naming the label."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/session_verified_kappa.feature"

[[row]]
id = "total-algebraic-path"
statement = "Every executed kernel is a hierarchy kernel — zero runtime float dispatch, parity with the retired float reference witnessed at gate time per (op, tier); held open by the pinned substrate's float-first dispatch for float dtypes, with the float-dispatch fraction of a compiled plan measured and reported, never asserted."
stage = "S3"
status = "open"
oracles = ["hologram-substrate"]
tier = "target"
executor = "rust"
feature = "features/suites/s3_execution/total_algebraic_path.feature"

[[row]]
id = "saturation-residency"
statement = "A failed verification UNPINS: the corrupted cache entry evaporates and resolution re-resolves once through the deeper tier (recorded provenance), so cache corruption degrades to a stream instead of dead-ending the journey; only the failing entry is evicted — every other cached κ (bound content) is untouched; without a deeper tier the failure stays loud naming the label, and recovered bytes must reproduce the κ before anything executes."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/saturation_residency.feature"

[[row]]
id = "derived-artifact-kappa"
statement = "The known set closes over deterministic derivation: a window's stage archives persist under their derivation key with recorded content-κs, a later session with identical inputs resolves them (content-verified at load) instead of re-deriving, resolution and derivation are output-identical, and a corrupted entry evaporates with derivation itself as the recovery — a wrong prior never dead-ends and never executes unverified content."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/derived_artifact_kappa.feature"

[[row]]
id = "idle-derivation"
statement = "Idle time feeds the anneal: between turns the session pre-derives the next window bucket's stage archives into the derived store — off the per-token path (no weights move, the resident window and its counters are untouched) — so a later bucket crossing resolves the window instead of compiling it on the critical path; abandoned speculation is ordinary derived content, evaporable by the same lifecycle; failed speculation is inert, never load-bearing."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/idle_derivation.feature"

[[row]]
id = "single-position-head"
statement = "The head computes only the position the sampler consumes: the pipeline gathers the consumed position's hidden state after the final norm (a runtime last_pos input the generation loop synthesizes by name, like every auxiliary), so per-step head cost is O(vocab·d) never O(window·vocab·d), the whole and chunked heads emit the same gathered form, staged equals monolithic through the gather, and greedy completions are unchanged by construction."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/single_position_head.feature"

[[row]]
id = "lazy-constant-residency"
statement = "The weight tier pages: a k-form archive loads against a κ-store provider with a residency budget instead of copying every weight resident — whole-κ weight constants become by_reference fingerprints (the fingerprint IS the κ's content digest, so the slot's label and every derivation key equal the fully-resident path's) that page in on first use and evict cold under the budget, verified once per κ per session at the trust boundary; the arena is a bounded window over the provider rather than a full copy, so peak resident weight bytes stay under the budget across a whole decode while the logits are byte-identical to the fully-resident load — residency is orthogonal to identity, the weight-tier analog of sub-tensor κ-resolution."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/lazy_constant_residency.feature"

[[row]]
id = "decode-plan"
statement = "The per-token pass computes one position: the decoder recipe emitted at seq = 1 with every fused attention node decomposed into masked past-attention over a fixed bucket of carried K/V rows — carried K/V is derived content through named ports (past_k/past_v in, k_new/v_new out, the engine splices each step's rows between steps), positions are runtime data (rope tables synthesized at the absolute position, an additive mask erasing unrealized bucket rows), one compiled artifact serves every step, bucket exhaustion is a geometric recompile never a ceiling, every step's logits match the whole-window plan at that position, and the carried rows persist across turns: a prompt extending the session's realized sequence rewinds to the shared prefix and steps only its novel suffix, indistinguishable from a fresh replay."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/decode_plan.feature"

[[row]]
id = "chunked-prefill"
statement = "Prefill amortizes the weight stream across a chunk: the decode plan generalized to seq = C emits C positions per pass over the same carried bucket (intra-chunk causality enters through the same additive mask that erases unrealized rows; rope tables arrive pre-expanded to the plan's head-major layout), a prompt suffix seeds in ceil(n/C) passes instead of n, a partial final chunk pads with rows that land above the realized length where the mask makes them unreachable until overwritten, and a chunk-seeded session is indistinguishable from a step-fed one at the sampler row and at every subsequent position."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/chunked_prefill.feature"

[[row]]
id = "speculative-decode"
statement = "A draftable stretch is decoded in batches without changing the output: the next tokens are drafted from the realized sequence's own recurrence (the tokens that followed the current suffix's most recent earlier occurrence — zero weights, no draft model), verified in ONE M=K pass whose head emits logits at every drafted position (the substrate's efficient batched matmul shape, not K single-position steps), and only the longest prefix the model would itself greedily produce is accepted — its K/V spliced from that same pass and one correcting bonus token committed — so the greedy completion is byte-identical to single-position decode. No recurrence means an empty draft and a plain step (never worse); a draft that verifies commits several tokens per two passes, so over a recurring stretch the forward-pass count falls below the token count. No unverified token is ever emitted."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/speculative_decode.feature"

[[row]]
id = "quantized-rest"
statement = "A quant-tiered model rests quantized: the download derives each fully-retirable projection's matmul-ready int8 artifact in the browser and evaporates its wide blob — the wide form transits once and rests nowhere (recorded provenance keeps it recoverable; a missing artifact re-derives at session warm, fail-closed on its recorded κ) — the κ-store holds the quantized form, and the chat journey completes on it with the tier stated in the narration, never silently."
stage = "S1"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s1_acquisition/quantized_rest.feature"

[[row]]
id = "quantized-transit"
statement = "A quantized weight form is derived content: computed deterministically from the wide tensor's κ (re-derivation reproduces the artifact κ bit-identically), addressed by its own κ, stored matmul-ready and bound by stage graphs as two ranged sub-tensor κ-bindings feeding Dequantize adjacent to its MatMul; once the derivation crystallizes the wide blob goes gas-phase — with wide content evicted the pipeline still generates and never resolves a wide projection κ — and the quantized staged pipeline generates identically to the quantized monolithic archive; quantized-vs-wide quality is measured, never asserted."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/quantized_transit.feature"

[[row]]
id = "chunked-head"
statement = "No head is too large to execute: the LM head partitions into vocab-row chunks at the pipeline's own stage granularity via κ-range bindings (a chunk binds a byte range of the whole tensor's κ; verification covers the whole content once, and it is the only whole-content read — a session-verified ranged binding rematerializes through resolve_range, moving only its slice), the concatenated chunk logits agree with the whole-head logits within kernel reduction-order tolerance with exact greedy-decode parity, no whole-vocabulary image is ever materialized, and a single-chunk head is exactly the classic head stage."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/chunked_head.feature"

[[row]]
id = "bounded-embedding"
statement = "A model is bounded by its unavoidable per-row/per-tile work, never whole-matrix F32 materialization: the embedding gathers the token rows at the table's native dtype and widens only the gathered result, so the whole [vocab, hidden] F32 image is never materialized, and a fused Phi3-family model executes monolithically and staged to identical logits."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/bounded_embedding.feature"

[[row]]
id = "kappa-provenance-resolution"
statement = "The κ-store is a cache over recorded provenance: a κ absent from the local store resolves from its revision-pinned source (verified by re-hashing to its κ), and the journey completes with an empty local cache — local and remote resolution are indistinguishable."
stage = "S3"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s3_execution/kappa_provenance_resolution.feature"

[[row]]
id = "kappa-materialization"
statement = "A k-form archive + a κ-store materialize into an executable session; every resolve is content-verified; a missing or corrupt κ aborts with the label."
stage = "S3"
status = "build"
oracles = ["blake3-kats"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/kappa_materialization.feature"

[[row]]
id = "execution-parity"
statement = "The materialized parametric pipeline reproduces ONNX Runtime logits for the same pinned model within tolerance."
stage = "S3"
status = "verified"
oracles = ["onnx-runtime", "smollm2-135m"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/execution_parity.feature"

[[row]]
id = "tokenizer-parity"
statement = "Encode/decode matches the reference HuggingFace tokenizers on the model's own published tokenizer.json."
stage = "S3"
status = "verified"
oracles = ["hf-tokenizers", "smollm2-135m"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/tokenizer_parity.feature"

[[row]]
id = "structural-za"
statement = "Addressed decode steps allocate zero runtime heap (class ZA) on the pinned substrate."
stage = "S3"
status = "verified"
oracles = ["hologram-substrate"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/structural_za.feature"

[[row]]
id = "structural-zm"
statement = "Execution moves no buffer bytes post-load (class ZM) on the pinned substrate."
stage = "S3"
status = "verified"
oracles = ["hologram-substrate"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/structural_zm.feature"

[[row]]
id = "structural-ce"
statement = "Unchanged compute cones are elided by κ-residency (class CE) on the pinned substrate."
stage = "S3"
status = "verified"
oracles = ["hologram-substrate"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/structural_ce.feature"

[[row]]
id = "structural-cf"
statement = "Only canonical forms cross the hologram boundary — closed OpKind catalog, interned shapes, canonical dtypes (class CF)."
stage = "S3"
status = "verified"
oracles = ["hologram-substrate"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/structural_cf.feature"

[[row]]
id = "structural-lw"
statement = "Every desugared lowering is held to the external reference of the op it replaces (class LW)."
stage = "S3"
status = "verified"
oracles = ["onnx-runtime"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/structural_lw.feature"

[[row]]
id = "structural-im"
statement = "Byte-level format parsing is confined to the import perimeter; nothing mid-pipeline parses raw model bytes (class IM)."
stage = "S3"
status = "build"
oracles = ["hologram-substrate"]
tier = "suite"
executor = "rust"
feature = "features/suites/s3_execution/structural_im.feature"

# ───────────────────────────── S4 — application ─────────────────────────────

[[row]]
id = "generation-loop"
statement = "Autoregressive generation is re-execution of the compiled graph with streamed token emission and config-driven sampling; it is bounded by stop conditions, the remaining context window, and cancellation — never a fixed token cap; no KV-cache machinery exists."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s4_application/generation_loop.feature"

[[row]]
id = "decode-elision"
statement = "Consecutive decode steps report elided (skipped) dispatches for the unchanged prefix cone — the measured k-scaling witness."
stage = "S4"
status = "build"
oracles = ["hologram-substrate"]
tier = "suite"
executor = "rust"
feature = "features/suites/s4_application/decode_elision.feature"

[[row]]
id = "app-domain-events"
statement = "The app-domain projection is pure and replayable: reduce(events) is deterministic and order-respecting."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s4_application/app_domain_events.feature"

[[row]]
id = "browser-journey"
statement = "Download → compile → materialize → run completes in real Chromium against the hermetic fixture, exercising the genuine workers/wasm/OPFS path."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/browser_journey.feature"

[[row]]
id = "chat-model-menu"
statement = "The chat interface hard-codes no models: its selection menu is populated SOLELY by the compiled archives in local storage (OPFS). A fresh instance offers nothing to chat with; a model merely added to the catalogue (or still downloading) never appears until it is compiled; a compiled model appears by exactly its on-disk archive path. Because browser \"clear site data\" does not reliably purge OPFS, the Models page lists everything actually stored and lets the user REMOVE it — removing a model clears it from storage and from the menu at once."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/chat_model_menu.feature"

[[row]]
id = "deep-model-journey"
statement = "The browser runs a checkpoint at PRODUCTION attention geometry — head_dim 128 (the real Qwen/Llama value), grouped-query attention, many stages, the int8 tier — end to end: download, compile, and a chat turn that COMPLETES and commits its assistant reply honestly (a turn that emits no text shows an explicit no-output notice, never a silent blank or a vanished turn). The committed handshake fixture runs at head_dim 16 and below the pool's parallel-dispatch threshold, so this is the first journey to exercise the real head_dim and an actual multi-worker GEMV dispatch; a worker that dies surfaces a diagnostic cause, never an opaque event."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/deep_model_journey.feature"

[[row]]
id = "scaled-rope-journey"
statement = "A checkpoint carrying an IMPLEMENTED rope_scaling law (llama3/yarn/linear/dynamic/longrope, and partial_rotary_factor) runs the whole browser journey — download, compile, chat streaming — with the engine synthesizing the scaled frequency tables at the realized positions; the law itself is pinned bit-level by the native rope_scaling_reference witnesses, and an UNIMPLEMENTED law is refused at preflight naming the type."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/scaled_rope_journey.feature"

[[row]]
id = "model-switch"
statement = "Switching models mid-session keeps chat streaming: two downloaded models are two directories, two archives, two sessions; the chat page switches between them (and back) without recompiling the world, wedging the stream store, or re-downloading anything already local."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/model_switch.feature"

[[row]]
id = "warm-turn"
statement = "A chat session's later turns run warm: the staged session — compiled window, resident stage sessions, session verified-κ set, derived-artifact cache — survives across sends, so a warm turn pays decode with zero window recompiles and zero stage rematerializations; the session rebuilds itself on model switch and a cold turn has identical semantics."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/warm_turn.feature"

[[row]]
id = "session-window"
statement = "A chat session is bounded only by the model's own context: the archive compiles at max_position_embeddings (staging absorbs the memory scaling), and a transcript that outgrows the window trims oldest-turn-first so the conversation continues — it never dead-ends."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/session_window.feature"

[[row]]
id = "chat-handshake"
statement = "The three-message handshake chat session completes in real Chromium on the hermetic fixture, matching the committed reference transcript deterministically."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/chat_handshake.feature"

[[row]]
id = "speculative-journey"
statement = "With speculative decode enabled, the browser's staged decode session drafts from the realized sequence's recurrence and verifies in one batched pass through a paged/staged verify pipeline, and the chat journey streams live and warm across turns over that path — the deployed browser integration of the natively byte-exact speculative decode (the batched shortcut changes speed, never meaning)."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/speculative_journey.feature"

[[row]]
id = "speculative-draft-pairing"
statement = "A catalogue-paired DRAFT model drives the browser drafter: the pairing is catalogue DATA (`draftModel`), downloading the target downloads the paired draft, and the worker builds it as a second decode session and `attach_draft`s it so speculative decode drafts from the paired model (`ModelDrafter`) instead of prompt-lookup. The pair shares ONE residency ledger (no over-commit of the wasm ceiling) and the draft must cover the target's vocabulary AND context (else the pairing is refused and prompt-lookup drafts) — the output is the target's byte for byte regardless (witnessed natively), so the pairing changes acceptance, never meaning."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/speculative_draft_pairing.feature"

[[row]]
id = "decode-bucket-growth"
statement = "A browser decode session that outgrows its fixed K/V bucket regrows it geometrically (64 → 128 → …) and keeps streaming: the regrowth frees the outgoing runner's resident stages and the stale prefill seeder BEFORE compiling the wider bucket, so the grow-only wasm linear memory never holds the old resident set and the new bucket's compilation at once (the over-commit that aborted a large model with a bare RuntimeError:unreachable). The residency invariant is witnessed natively (footprint zero at each rebuild); this witnesses the real wasm decode path crossing a bucket boundary and streaming on."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/decode_bucket_growth.feature"

[[row]]
id = "live-model-journey"
statement = "The full journey (download → compile → run → handshake) completes in real Chromium on the pinned SmolLM2-135M-Instruct."
stage = "S4"
status = "verified"
oracles = ["smollm2-135m", "hf-hub"]
tier = "suite"
executor = "browser"
feature = "features/suites/s4_application/live_model_journey.feature"

[[row]]
id = "deployment-gate"
statement = "The Pages deployment requires the journey gate: the deploy workflow cannot publish without the browser suites green."
stage = "S4"
status = "build"
oracles = ["journey-reference"]
tier = "suite"
executor = "rust"
feature = "features/suites/s4_application/deployment_gate.feature"

[[row]]
id = "performance-contract"
statement = "Throughput, compile-time, and reuse floors — measured and reported per environment, never asserted as invariants."
stage = "S4"
status = "open"
oracles = ["hologram-substrate"]
tier = "target"
executor = "rust"
feature = "features/suites/s4_application/performance_contract.feature"
